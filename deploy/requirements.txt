# ============================================
# Python 版本要求: 3.10
# 注意: chumpy 和 manotorch 在 Python 3.11+ 上有兼容性问题
# ============================================

# Web 服务
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
psycopg2-binary>=2.9.11

# 数据处理
numpy>=1.24.0,<1.27.0
pyarrow>=10.0.0
pylance>=0.9.0
pyyaml>=6.0

# 数据集加载
huggingface_hub>=0.19.0
datasets>=2.0.0,<3.2.0
tqdm

# 科学计算
scipy>=1.7.0
six>=1.16.0

# 深度学习（CPU-only，通过 Dockerfile 单独安装）

# 3D 处理
trimesh>=3.20.0
pin==2.6.21

# 图像处理
opencv-python-headless>=4.8.0
Pillow>=10.0.0

# XML 处理（URDF）
lxml>=4.9.0

# MANO 依赖（通过 git 安装，见 Dockerfile）
# pip install --no-build-isolation git+https://github.com/mattloper/chumpy
# pip install git+https://github.com/lixiny/manotorch.git
